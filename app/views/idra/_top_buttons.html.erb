<div class="div-buttons" style="width: 100%">
  <% if current_user %>
    <button id="openModalButton" class="button modal-toggle-button" type="button" data-idra-modal-open>
      <span style="font-size:22px; color: var(--idra-star-color); line-height: 1;">★</span>
      <span id="count" data-idra-counter><%= @element_count %></span>
    </button>
  <% end %>
  <%= render partial: "pagination" %>
</div>
