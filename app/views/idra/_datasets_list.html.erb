<div id="datasets-list">
  <h2 class="text-center">Saved Datasets</h2>
  <input type="text" id="search-bar" placeholder="Search Datasets">
  <br><br>
  <div class="scrollable">
    <% if @datasets.present? %>
      <% @datasets.each do |dataset| %>
        <div class="dataset-item">
          <a href='<%= dataset.url %>' target="_blank" rel="nofollow"><%= dataset.title %></a>
          <%= button_tag type: "button", class: "button delete-btn", data: { dataset_delete: true, dataset_id: dataset.dataset_id }, aria: { label: "Delete dataset" } do %>
            <%= icon "delete-bin-line", class: "delete-icon" %>
            <span class="sr-only">Delete</span>
          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
